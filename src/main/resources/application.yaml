server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: http://github.com/dmanias/microdemo-spring-configuration
          clone-on-start: true